{
  "name": "@overstory/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "NODE_OPTIONS=--network-family-autoselection-attempt-timeout=2000 drizzle-kit migrate",
    "db:push": "NODE_OPTIONS=--network-family-autoselection-attempt-timeout=2000 drizzle-kit push",
    "db:studio": "NODE_OPTIONS=--network-family-autoselection-attempt-timeout=2000 drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
