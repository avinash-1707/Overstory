// AnalysisArtifact — engine layer, append-only, NEVER served to agents. D16.
// TODO(schema): id, flowId (FK), version, supersedesVersionId, content (jsonb),
//   seededFrom (jsonb provenance), createdAt.
export {}
