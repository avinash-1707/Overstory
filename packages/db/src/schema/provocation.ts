// Provocation — capture history. D5/D16.
// TODO(schema): id, flowId, decisionId, alternative, tradeoffs, outcome
//   (accepted|rejected_with_reason|pending), producedRationale, createdAt.
export {}
