// ContradictionEvent — freshness trigger. D11.
// TODO(schema): id, decisionId (FK), trigger (agent_edit|pr|manual), detail,
//   changedFiles[], detectedAt, resolution (still_true|superseded|pending), resolvedDecisionId.
export {}
