// Analysis — deep flow understanding (engine layer, D16) + pointer resolution via ts-morph (D15).
// Builds the append-only AnalysisArtifact; never served to agents.
// TODO: analyzeFlow, resolveSymbols (ts-morph), detectDrift.
export {}
