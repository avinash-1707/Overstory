// LLM — managed metered Anthropic access (D25). Overstory holds the key, meters, bills.
// Opus-class for analysis + provocation (quality-critical); Haiku/Sonnet for seeding,
// ranking, contradiction triage. See capture-loop.md cost table.
// TODO: anthropic client wrapper, model tiers, token metering hook.
export {}
