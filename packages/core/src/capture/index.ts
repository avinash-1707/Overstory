// Capture loop — the moat (D4/D16/D18). See docs/technical/capture-loop.md.
// Pipeline: discover flows -> deep analysis -> seed -> rank & budget -> provoke -> defend -> persist.
// TODO: flow-discovery, seeding, ranking, provoke, defend stages.
export {}
